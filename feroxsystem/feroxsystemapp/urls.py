from django.urls import path
from . import views
from django.conf.urls import url, include

"""
urlpatterns = [
    path('login/', views.login, name='login'),
    path('home/', views.formview, name='home'),
    path('logout/', views.logout, name='logout')
]
"""
"""
urlpatterns = [
    path('', views.index, name='index')
]
"""
"""
urlpatterns = [
    path('login/', views.login, name='login'),
    path('logout/', views.logout, name='logout')
]
"""